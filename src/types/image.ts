export type Urls = {
    full: string;
    raw: string;
    regular: string;
    small: string;
    small_s3: string;
    thumb: string;
}

export type User = {
    id: string;
    name: string;
    first_name: string;
    last_name: string;
    username: string;
    bio: string;
    portfolio_url: string;
    total_photos: number;
    total_promoted_photos: number;
}